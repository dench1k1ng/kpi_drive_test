import 'package:flutter/material.dart';

class KpiColors {
  // Зеленый KPI - выполнение/факт
  static const Color green = Color(0xFF4CAF50);
  // Желтый KPI - денежное вознаграждение / в процессе
  static const Color yellow = Color(0xFFFDD835);
  // Красный KPI - невыполнение / задача
  static const Color red = Color(0xFFD32F2F);
  // Синий KPI - показатели
  static const Color blue = Color(0xFF1E88E5);

  // Фон карточек
  static const Color cardBackground = Color(0xFF2C2C2C);
  // Фон Канбан колонок
  static const Color columnBackground = Color(0xFF242424);
  // Основной темный фон
  static const Color background = Color(0xFF1B1B1B);
}
